---
server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/nicmaster/spring-cloud-configserver-file
          search-paths:
            - 'station*'
